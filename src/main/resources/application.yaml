server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/anki?currentSchema=statistics
    username: postgres
    password: postgres
  flyway:
    schemas: statistics
    user: postgres
    password: postgres
  jpa:
    show-sql: true
#    open-in-view: false
